import { Builder, builder } from '@builder.io/sdk';
import { builder as builderReact } from '@builder.io/react';

// Inicializar Builder.io
builderReact.init('e413bf72af054b07925671fff35fae24');

// Nota: Los componentes personalizados se registran en src/components/BuilderComponents.jsx
// Este archivo se mantiene para compatibilidad con Builder.io 